{
  "name": "@wixc3/sample-monorepo-publish-test-components",
  "version": "1.0.17",
  "module": "src/index",
  "main": "src/index",
  "scripts": {
    "clean": "rimraf ./dist",
    "prebuild": "yarn clean",
    "repack": "node -r @ts-tools/node/r ../../scripts/build.ts",
    "test": "mocha -r @ts-tools/node/r \"test/**/*.spec.ts?(x)\" --watch-extensions ts,tsx --colors",
    "prepack": "yarn clean && yarn repack"
  },
  "peerDependencies": {
    "react": ">=16"
  },
  "license": "MIT",
  "repository": "git@github.com:wixplosives/sample-monorepo.git",
  "sideEffects": false,
  "publishConfig": {
    "access": "restricted"
  }
}
