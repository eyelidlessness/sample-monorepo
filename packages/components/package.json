{
  "name": "@wixc3/sample-monorepo-publish-test-components",
  "version": "1.0.14",
  "module": "src/index",
  "main": "src/index",
  "scripts": {
    "clean": "rimraf ./dist",
    "prebuild": "yarn clean && rsync -r ./ ./dist --exclude '*config.json'",
    "build": "tsc -p .",
    "test": "mocha -r @ts-tools/node/r \"test/**/*.spec.ts?(x)\" --watch-extensions ts,tsx --colors",
    "prepack": "yarn build && node ../../scripts/build.js "
  },
  "peerDependencies": {
    "react": ">=16"
  },
  "files": [
    "./dist"
  ],
  "license": "MIT",
  "repository": "git@github.com:wixplosives/sample-monorepo.git",
  "sideEffects": false,
  "publishConfig": {
    "access": "restricted"
  },
  "gitHead": "1f76b2920072edddb660438109e71228d9a78c0a"
}
